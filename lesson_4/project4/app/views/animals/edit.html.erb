<%= form_with(model: @animal, local: true) do |form| %>

<p>
	<%= form.label :name %>
	<%= form.text_field :name %>
</p>

<p>
	<%= form.label :text %>
	<%= form.text_area :text %>
</p>

<p>
	<%= form.submit %>
</p>

<% end %>